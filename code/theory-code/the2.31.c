#include <stdio.h>

int main (void)

{
	int n, s, c;
	
	n = 0;
	s = n * n;
	c = s * n;
	printf ("number\tsquare\tcube\n");
	printf ("%d\t%d\t%d\n", n, s, c);
	
	n = n + 1;
	s = n * n;
	c = s * n;
	printf ("%d\t%d\t%d\n", n, s, c);
	
	n = n + 1;
	s = n * n;
	c = s * n;
	printf ("%d\t%d\t%d\n", n, s, c);
	
	n = n + 1;
	s = n * n;
	c = s * n;
	printf ("%d\t%d\t%d\n", n, s, c);
	
	n = n + 1;
	s = n * n;
	c = s * n;
	printf ("%d\t%d\t%d\n", n, s, c);
	
	n = n + 1;
	s = n * n;
	c = s * n;
	printf ("%d\t%d\t%d\n", n, s, c);
	
	n = n + 1;
	s = n * n;
	c = s * n;
	printf ("%d\t%d\t%d\n", n, s, c);
	
	n = n + 1;
	s = n * n;
	c = s * n;
	printf ("%d\t%d\t%d\n", n, s, c);
	
	n = n + 1;
	s = n * n;
	c = s * n;
	printf ("%d\t%d\t%d\n", n, s, c);
	
	n = n + 1;
	s = n * n;
	c = s * n;
	printf ("%d\t%d\t%d\n", n, s, c);
	
	n = n + 1;
	s = n * n;
	c = s * n;
	printf ("%d\t%d\t%d\n", n, s, c);
	
	return 0;
}